# Wrkstrm

@Metadata {
  @PageKind(article)
  @PageColor(blue)
  @TitleHeading("Engineering Workstream")
  @PageImage(purpose: icon, source: "workstreams-wrkstrm-brand-icon", alt: "Wrkstrm Icon")
  @PageImage(purpose: card, source: "workstreams-wrkstrm-brand-card", alt: "Wrkstrm Card")
}

@Options {
  @AutomaticSeeAlso(disabled)
}


The engineering workstream for developer tools and libraries.

@Image(source: "workstreams-wrkstrm-brand-hero", alt: "Wrkstrm Hero")

## Core Workstream

- **Name:** Wrkstrm
- **Reverse-DNS Root:** `app.wrkstrm` (for tools/infrastructure)
- **Archetype:** The Terminal / The Workbench

## Location

The detailed brand spec for Wrkstrm lives in the canonical brand bundle:
`docc/brand-identities.docc`

## Visuals (Summary)

- **Primary Color:** Blue (`#0071e3`)
- **Logo:** Terminal Window (`concept-terminal-block-v2`)
- **Motif:** Tool-centric, inverted-W signature.

## Usage

Used for internal tooling, CLIs, shared libraries, and the `foundry` system.
