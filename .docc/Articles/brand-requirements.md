# brand requirements

@Metadata {
  @PageImage(purpose: icon, source: "Articles-brand-requirements-icon", alt: "brand requirements icon")
  @PageImage(purpose: card, source: "Articles-brand-requirements-card", alt: "brand requirements card")
  @PageKind(article)
  @PageColor(orange)
}
  Define every brand using the same checklist, so identities stay comparable,
  audit-ready, and easy to maintain.
  ## Required sections
  - **Core identity** (name, handle, reverse-DNS root, archetype)
  - **Color** (primary/secondary palette, usage rules, contrast targets)
  - **Typography** (families, weights, sizes, hierarchy)
  - **Logo** (primary, alternate, spacing, misuse rules)
  - **Iconography** (style rules, stroke/weight, usage)
  - **Layout grid** (spacing scale, layout rules, breakpoints)
  - **Motion** (timing, easing, preferred patterns)
  - **Accessibility** (contrast, motion reduction, semantic rules)
  - **Voice & tone** (copy guidance, do/don’t)
  - **Examples** (approved references, mockups)
  - **Assets** (download links, source files, licensing)
  ## Governance
  - Keep all brand pages kebab-case and lowercase.
- Store assets in the same bundle under `Resources/` when applicable.
- Use resource names relative to the bundle; do not prefix with the bundle name.
  - Prefer DocC Topics to make navigation consistent.
}

@Image(source: "Articles-brand-requirements-hero", alt: "brand requirements hero")
